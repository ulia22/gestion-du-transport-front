import { JsontoDatePipe } from './jsonto-date.pipe';

describe('JsontoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new JsontoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
