import { ImmatriculationPipe } from './immatriculation.pipe';

describe('ImmatriculationPipe', () => {
  it('create an instance', () => {
    const pipe = new ImmatriculationPipe();
    expect(pipe).toBeTruthy();
  });
});
